# Godot
.godot/
.import/
export_presets.cfg.uid
*.translation

# Generated Android gradle build template (regenerated by tools/setup_godot.sh)
android/build/
android/.build_version

# Build output
build/*.apk
build/*.aab

# Keystore (debug keystore below is intentionally committed for local
# reproducible sideload builds; ignore anything else dropped here)
keystore/*.jks
keystore/release.keystore
